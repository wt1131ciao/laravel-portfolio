# CLAUDE.md

## コミット規約

### 基本方針
- 1コミット = 1つの意味のある変更単位（機能追加・バグ修正・設定変更などを混在させない）
- こまめにコミットする（大きな変更をまとめて1コミットにしない）

### コミットメッセージ形式
[Conventional Commits](https://www.conventionalcommits.org/) に従う。

```
<type>: <概要>
```

#### type 一覧
| type | 用途 |
|------|------|
| `feat` | 新機能の追加 |
| `fix` | バグ修正 |
| `docs` | ドキュメントのみの変更 |
| `style` | コードの動作に影響しない変更（フォーマット等） |
| `refactor` | バグ修正でも機能追加でもないコード変更 |
| `test` | テストの追加・修正 |
| `chore` | ビルドプロセスや補助ツールの変更（依存関係更新など） |

#### 例
```
feat: ユーザー認証機能を追加
fix: ログイン時のセッションエラーを修正
docs: READMEにセットアップ手順を追記
chore: docker-compose.yaml を追加
```

### NG パターン
- `update files` など意味のない概要
- 複数の独立した変更を1コミットに混ぜる
- ステージの状態を確認せずに `git add .` でまとめてコミット
